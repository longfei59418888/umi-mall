import { ParamsType } from '@ant-design/pro-provider';

/**
 *
 * @param params 参数按实际的请求参数
 *
 */
export async function requestDemo(params: ParamsType) {
  /** 这里替换成请求接口 */
  return [];
}

export async function deleteDemo(id: string | number) {
  /** 这里替换成请求接口 */
  return [];
}
